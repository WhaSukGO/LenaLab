# Copy to .env and fill in. .env is gitignored — never commit your real key.
# Needed only for LIVE agent runs (Track A committee, Track B authoring). Offline tests,
# calibration, and demo rendering need no key.
ANTHROPIC_API_KEY=
